<div class="col-md-12 col-xs-12">
  <div class="x_panel">
    <div class="x_title">
      <h2>New item</h2>
      <div class="clearfix"></div>
    </div>
    <div class="x_content">
      <%= form_for @item, :url => url_for(:controller => 'item', :action => 'create'), :html => {:class => 'form-horizontal form-label-left input_mask'} do |f| %>
      <% if @item.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@item.errors.count, "error") %> prohibited this customer from being saved:</h2>

          <ul>
          <% @item.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

        <div class="form-group">
          <label class="control-label col-md-3 col-sm-3 col-xs-12">Name</label>
          <div class="col-md-9 col-sm-9 col-xs-12">
            <%= f.text_field :name, :class => 'form-control', :placeholder => 'Name' %>
          </div>
        </div>

        <div class="form-group">
          <label class="control-label col-md-3 col-sm-3 col-xs-12">Price</label>
          <div class="col-md-9 col-sm-9 col-xs-12">
            <%= f.number_field :price, :class => 'form-control money', :step => "0.01", required: 'true' %>
          </div>
        </div>

        <div class="form-group">
          <label class="control-label col-md-3 col-sm-3 col-xs-12">Description</label>
          <div class="col-md-9 col-sm-9 col-xs-12">
            <%= f.text_field :description, :class => 'form-control', :placeholder => 'Description' %>
          </div>
        </div>

        <div class="ln_solid"></div>
        <div class="form-group">
          <div class="col-md-9 col-sm-9 col-xs-12 col-md-offset-3">
            <%= link_to 'Cancel', :back, :class => 'btn btn-primary' %>
            <%= f.submit 'Save', :class => 'btn btn-success' %>
          </div>
        </div>

      <% end %>
    </div>
  </div>